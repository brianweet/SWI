function [ fcurrent, fbest, xbest, quit ] = evaluate_function( fun, p, fbest, xbest )
%EVALUATE_FUNCTION Summary of this function goes here
%   Detailed explanation goes here

    fcurrent = feval(FUN, p);
    if fcurrent < fbest
        fbest = fcurrent;
        xbest = p;
        if fbest <= ftarget
            
        end
    end
end

